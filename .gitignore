.venv/
__pycache__/
*.pyc
*.egg-info/
# Everything under models/ is fetched by scripts/fetch_models.py and is not source.
# Weights, tokenisers and configs alike - if it can be re-downloaded, it does not
# belong in the history.
models/*
!models/.gitkeep
.setu_cache/
setu.db
bench_results/
.pytest_cache/

.claude/
ui/node_modules/

# npm deps pulled in only to build the submission documents
node_modules/
package-lock.json
package.json
